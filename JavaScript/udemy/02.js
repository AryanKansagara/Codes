const str = "Hello world !";
console.log(str.slice(0,3));

// stringName.slice(x,y);
// x = starting index and y = ending index

// ---------------------------------

// changing cases

console.log(str.toUpperCase());

console.log(str.toLowerCase());

const username = "ARYAN kANSAGARA";

console.log(username.slice(0,1).toUpperCase()+username.slice(1,username.length).toLowerCase());